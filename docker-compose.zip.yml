version: "3"

services:
  zipper:
    build:
      context: tools
      dockerfile: zip.dockerfile
    entrypoint: bash
    command: make-zip.sh /src
    working_dir: /src/bin
    volumes:
      - "./:/src"
    restart: "no"
